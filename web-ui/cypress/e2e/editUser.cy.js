sessionStorage.setItem('Access', "CompleteAccess")

describe('Edit users end to end tests', () => {
  it('Verificar happy path (cambio de numero)', () => {
    const randomNumber = Math.floor(Math.random() * 100);s
    
  });
});

